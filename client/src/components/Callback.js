import React, { Component } from "react";

export default class Callback extends Component {
    render() {
        return(
        <div>
            {this.props.name}
            Loading...
        </div>
        )
    }
}